{
  "ChangeBatch": {
    "Changes": [
      {
        "Action": "{{action}}",
        "ResourceRecordSet": {
          "Name": "{{recordName}}",
          "Type": "A",
          "AliasTarget": {
            "DNSName": "{{dnsName}}",
            "EvaluateTargetHealth": false,
            "HostedZoneId": "{{hostedZoneIdRecord}}"
          }
        }
      }
    ]
  },
  "HostedZoneId": "{{hostedZoneIdRecord}}"
}
